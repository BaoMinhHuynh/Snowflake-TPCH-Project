version: 2

models:
  - name: dim_order_line_fulfillment
    description: Order line fulfillment attributes
    columns:
      - name: order_line_fulfillment_key
        tests:
          - not_null
          - unique

      - name: return_flag
        tests:
          - not_null
          - accepted_values:
              values: ["R", "A", "N"]

      - name: order_line_status
        tests:
          - not_null
          - accepted_values:
              values: ["O", "F"]

      - name: ship_mode
        tests:
          - not_null
